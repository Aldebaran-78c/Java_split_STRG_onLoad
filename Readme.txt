These two file contain are taken from an existing project and are meant to be used for schooling purpuses and discussion among the students. 

Log_Loader.java
This can be used to load a string from a given file.
The method loads a pre-defined file since it was part of a larger project.
The filepath and filename variables may have to be changed depending on the needs.

SplitJournal.java
The methods given here do split the loaded string and fill a collection with the string contents of the lines from the original files, which then can become subject to further procedures of string analysis.